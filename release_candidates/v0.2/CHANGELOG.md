# Changes for 0.2.0-rc.1

This is a candidate changelog, not a published-release announcement.

## Assurance

Replace the axiom-log checker with strict parsing and registry validation;
expand its defensive fixtures from two to forty; keep explicit legacy registry
support without claiming per-row classifications that are absent.

Harden the reviewed core CI entry point and disable the reviewed legacy
secret-bearing autonomous-engine job pending architectural remediation.
Preserve all existing proof/consistency gates. Add 15 integration/failure-path
fixtures and a separate read-only job for the Init-only Lean candidates.

## Research intake

Add six isolated elementary Lean declarations with an exact candidate-only
axiom registry. Their first GitHub CI compilation and audit passed on Lean
4.31.0, with no axioms reported for any of the six declarations. Record the
source-bound observation in verification.json; retain zero canonical promotions.

Add reproducible synthetic negative controls, the positive anchored uniqueness
result, and an exact integer/rational bound component. Keep structured knowledge
records with separate meaning, verification status, limitations, prerequisites,
and promotion requirements. The analytic cycle-label claim remains unformalized.

## Public statements

Label the version as a candidate; qualify proof/trust rhetoric; remove unverified
literature priority; preserve separate ECDLP and ResearchOS ledgers and canonical
counts. Toolchain and Mathlib configuration are unchanged.

## Integration and remaining gates

The initial implementation was written as commit
084e0e5eafd95ec74cd8237c1c0e5aef174e1a81 and draft PR #432. The initial
local preparation lacked Lean; the subsequent CI result supersedes that
preparation limitation for the six candidates only.

Full repository checks, both canonical axiom audits, clean release rebuild,
generated-artifact closure, scientific review, and broader workflow/dependency
review are separate obligations. No merge, tag, release, stacked-research-branch
integration, or parity-lab change is performed by this candidate.
