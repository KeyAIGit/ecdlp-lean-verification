# Transient prover work files (created in CI / local runs, never committed)
ProverLoopAttempt.lean
ProverTargetAttempt.lean
prover-loop-report.md
prover-target-report.md
prover-target-success.lean
featherless_models.json

# Lean build output
/.lake/
/build/

# Python
__pycache__/
*.pyc

# Agent context packs — generated on demand by scripts/export_agent_bundle.py.
# Only bundles/MANIFEST.json is committed; the inlined packs would duplicate repo
# content and drift, so they are regenerated fresh whenever needed.
bundles/small.md
bundles/medium.md
bundles/large.md

# Next.js / TypeScript build artifacts for the phase-2 platform scaffold
# (auto-generated by the toolchain, never committed — standard Next.js ignores).
platform/next-env.d.ts
platform/.next/
*.tsbuildinfo

# Local v0.2 validation logs; never canonical proof evidence by themselves.
_v02_validation/
